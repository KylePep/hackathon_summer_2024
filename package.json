
{
  "name": "codefitness",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd CodeFitness.client && npm i",
    "setup:server": "cd CodeFitness && npm i",
    "start": "node CodeFitness/index.js"
  },
  "engines": {
    "node": "^16"
  }
}